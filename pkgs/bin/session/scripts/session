#!/bin/sh -l

reset
clear

unset LANG
unset LOGIN_SCRIPT

export XDG_SESSION_ID=$$
export TMPDIR="${TMPDIR}/$(id -u)/${XDG_SESSION_ID}"
export XDG_DATA_DIRS="/ix/realm/${USER}/share"
export XDG_RUNTIME_DIR="${TMPDIR}"

mkdir -m 0700 -p ${XDG_RUNTIME_DIR}

exec user-session
